export API_URL_UNTRIMMED=$(echo $PLATFORM_ROUTES | base64 --decode | jq -r 'to_entries[] | select (.value.id == "client") | .key')
export NEXT_PUBLIC_API_URL=$(echo "${API_URL_UNTRIMMED::-1}")
export PREVIEW_SECRET=mysecret
